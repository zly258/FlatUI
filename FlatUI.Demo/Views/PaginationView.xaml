<UserControl x:Class="FlatUI.Demo.Views.PaginationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:flat="clr-namespace:FlatUI.Library.Controls;assembly=FlatUI.Library">
    <ScrollViewer Margin="20">
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="分页" FontSize="24" FontWeight="Bold"/>
                <Button Content="查看源码" Style="{DynamicResource FlatButton}" Click="ViewSource_Click" Margin="20,0,0,0"/>
            </StackPanel>
            
            <!-- Basic Pagination -->
            <HeaderedContentControl Header="基础分页" Style="{DynamicResource FlatCard}" Margin="0,0,0,20">
                <StackPanel Margin="10">
                    <flat:Pagination x:Name="BasicPagination" TotalCount="100" PageSize="10" Margin="0,0,0,10"/>
                </StackPanel>
            </HeaderedContentControl>
            
            <!-- With Total Count -->
            <HeaderedContentControl Header="显示总数" Style="{DynamicResource FlatCard}" Margin="0,0,0,20">
                <StackPanel Margin="10">
                    <flat:Pagination TotalCount="500" PageSize="20" ShowTotal="True" Margin="0,0,0,10"/>
                </StackPanel>
            </HeaderedContentControl>
            
            <!-- With Jump -->
            <HeaderedContentControl Header="带跳转" Style="{DynamicResource FlatCard}" Margin="0,0,0,20">
                <StackPanel Margin="10">
                    <flat:Pagination TotalCount="200" PageSize="10" ShowTotal="True" ShowJump="True" Margin="0,0,0,10"/>
                </StackPanel>
            </HeaderedContentControl>
            
            <!-- Small Size -->
            <HeaderedContentControl Header="小尺寸" Style="{DynamicResource FlatCard}">
                <StackPanel Margin="10">
                    <flat:Pagination TotalCount="50" PageSize="5" ShowTotal="True" ShowJump="True" Margin="0,0,0,10"/>
                </StackPanel>
            </HeaderedContentControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
