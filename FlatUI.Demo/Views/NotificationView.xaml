<UserControl x:Class="FlatUI.Demo.Views.NotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:flat="clr-namespace:FlatUI.Library.Controls;assembly=FlatUI.Library">
    <ScrollViewer Margin="20">
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="通知" FontSize="24" FontWeight="Bold"/>
                <Button Content="查看源码" Style="{DynamicResource FlatButton}" Click="ViewSource_Click" Margin="20,0,0,0"/>
            </StackPanel>
            
            <!-- Notification Types -->
            <HeaderedContentControl Header="通知类型" Style="{DynamicResource FlatCard}" Margin="0,0,0,20">
                <StackPanel Margin="10">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="信息" Style="{DynamicResource FlatButton}" Click="ShowInfo_Click" Margin="0,0,10,0"/>
                        <Button Content="成功" Style="{DynamicResource FlatButton}" Click="ShowSuccess_Click" Margin="0,0,10,0"/>
                        <Button Content="警告" Style="{DynamicResource FlatButton}" Click="ShowWarning_Click" Margin="0,0,10,0"/>
                        <Button Content="错误" Style="{DynamicResource FlatButton}" Click="ShowError_Click"/>
                    </StackPanel>
                </StackPanel>
            </HeaderedContentControl>
            
            <!-- System Notification -->
            <HeaderedContentControl Header="系统通知" Style="{DynamicResource FlatCard}" Margin="0,0,0,20">
                <StackPanel Margin="10">
                    <Button Content="显示系统通知" Style="{DynamicResource FlatButton}" Click="ShowSystem_Click"/>
                </StackPanel>
            </HeaderedContentControl>
            
            <!-- Notification Position -->
            <HeaderedContentControl Header="通知位置" Style="{DynamicResource FlatCard}">
                <StackPanel Margin="10">
                    <TextBlock Text="通知默认显示在右上角" Margin="0,0,0,10"/>
                </StackPanel>
            </HeaderedContentControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
