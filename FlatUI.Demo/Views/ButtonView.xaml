<UserControl x:Class="FlatUI.Demo.Views.ButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:flat="clr-namespace:FlatUI.Library.Controls;assembly=FlatUI.Library">
    <ScrollViewer Margin="20">
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="按钮" FontSize="24" FontWeight="Bold"/>
                <Button Content="查看源码" Style="{DynamicResource FlatButton}" Click="ViewSource_Click" Margin="20,0,0,0"/>
            </StackPanel>
            
            <!-- Primary Buttons -->
            <HeaderedContentControl Header="主要按钮" Style="{DynamicResource FlatCard}" Margin="0,0,0,20">
                <StackPanel Margin="10" Orientation="Horizontal">
                    <Button Content="主要按钮" Style="{DynamicResource PrimaryButton}" Margin="0,0,10,0"/>
                    <Button Content="小尺寸" Style="{DynamicResource PrimaryButton}" Height="28" FontSize="12" Margin="0,0,10,0"/>
                    <Button Content="大尺寸" Style="{DynamicResource PrimaryButton}" Height="40" FontSize="16" Margin="0,0,10,0"/>
                    <Button Content="禁用状态" Style="{DynamicResource PrimaryButton}" IsEnabled="False"/>
                </StackPanel>
            </HeaderedContentControl>
            
            <!-- Default Buttons -->
            <HeaderedContentControl Header="默认按钮" Style="{DynamicResource FlatCard}" Margin="0,0,0,20">
                <StackPanel Margin="10" Orientation="Horizontal">
                    <Button Content="默认按钮" Style="{DynamicResource FlatButton}" Margin="0,0,10,0"/>
                    <Button Content="小尺寸" Style="{DynamicResource FlatButton}" Height="28" FontSize="12" Margin="0,0,10,0"/>
                    <Button Content="大尺寸" Style="{DynamicResource FlatButton}" Height="40" FontSize="16" Margin="0,0,10,0"/>
                    <Button Content="禁用状态" Style="{DynamicResource FlatButton}" IsEnabled="False"/>
                </StackPanel>
            </HeaderedContentControl>
            
            <!-- Icon Buttons -->
            <HeaderedContentControl Header="图标按钮" Style="{DynamicResource FlatCard}" Margin="0,0,0,20">
                <StackPanel Margin="10" Orientation="Horizontal">
                    <Button Style="{StaticResource IconButton}" Content="M12 22c1.1 0 2-.9 2-2h-4c0 1.1.89 2 2 2zm6-6v-5c0-3.07-1.64-5.64-4.5-6.32V4c0-.83-.67-1.5-1.5-1.5s-1.5.67-1.5 1.5v.68C7.63 5.36 6 7.92 6 11v5l-2 2v1h16v-1l-2-2z" Margin="0,0,10,0"/>
                    <Button Style="{StaticResource IconButton}" Content="M3 17.25V21h3.75L17.81 9.94l-3.75-3.75L3 17.25zM20.71 7.04c.39-.39.39-1.02 0-1.41l-2.34-2.34c-.39-.39-1.02-.39-1.41 0l-1.83 1.83 3.75 3.75 1.83-1.83z" Margin="0,0,10,0"/>
                    <Button Style="{StaticResource IconButton}" Content="M6 19c0 1.1.9 2 2 2h8c1.1 0 2-.9 2-2V7H6v12zM19 4h-3.5l-1-1h-5l-1 1H5v2h14V4z" Margin="0,0,10,0"/>
                </StackPanel>
            </HeaderedContentControl>
            
            <!-- Danger Button -->
            <HeaderedContentControl Header="危险按钮" Style="{DynamicResource FlatCard}" Margin="0,0,0,20">
                <StackPanel Margin="10" Orientation="Horizontal">
                    <Button Content="危险按钮" Style="{DynamicResource DangerButton}" Margin="0,0,10,0"/>
                    <Button Content="危险描边" Style="{DynamicResource DangerOutlineButton}"/>
                </StackPanel>
            </HeaderedContentControl>
            
            <!-- Button States -->
            <HeaderedContentControl Header="按钮状态" Style="{DynamicResource FlatCard}">
                <StackPanel Margin="10">
                    <TextBlock Text="鼠标悬停查看状态变化" Margin="0,0,0,10"/>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="主要按钮" Style="{DynamicResource PrimaryButton}" Margin="0,0,10,0"/>
                        <Button Content="默认按钮" Style="{DynamicResource FlatButton}" Margin="0,0,10,0"/>
                        <Button Content="危险按钮" Style="{DynamicResource DangerButton}" Margin="0,0,10,0"/>
                        <Button Content="成功按钮" Background="{DynamicResource SuccessBrush}" Foreground="White" Style="{DynamicResource FlatButton}"/>
                    </StackPanel>
                </StackPanel>
            </HeaderedContentControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
