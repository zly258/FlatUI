<UserControl x:Class="FlatUI.Library.Controls.NotificationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="Auto" Width="300" Margin="0,5">
    <Border Background="{StaticResource BackgroundBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="2">
        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Path x:Name="Icon" Data="M12,2C6.48,2 2,6.48 2,12s4.48,10 10,10 10-4.48 10-10S17.52,2 12,2z M13,17h-2v-6h2v6z m0-8h-2V7h2v2z"
                  Fill="{StaticResource InfoBrush}" Width="20" Height="20" Stretch="Uniform" VerticalAlignment="Top"/>
            <StackPanel Grid.Column="1" Margin="10,0">
                <TextBlock x:Name="TitleText" FontWeight="Bold" Text="Notification"/>
                <TextBlock x:Name="MessageText" TextWrapping="Wrap" Text="This is a notification message."/>
            </StackPanel>
            <Button Grid.Column="2" Content="✕" Style="{StaticResource FlatButton}" Padding="5" Height="24" Width="24"
                    VerticalAlignment="Top" Click="Close_Click"/>
        </Grid>
    </Border>
</UserControl>