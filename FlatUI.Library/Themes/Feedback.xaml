<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Brushes.xaml"/>
        <ResourceDictionary Source="Fonts.xaml"/>
        <ResourceDictionary Source="Shared.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- Spinner Style -->
    <Style x:Key="FlatSpinner" TargetType="Control">
        <Setter Property="Width" Value="32"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Control">
                    <Grid>
                        <Ellipse Stroke="{StaticResource BorderBrush}" StrokeThickness="3"/>
                        <Path x:Name="path" Stroke="{StaticResource PrimaryBrush}" StrokeThickness="3" 
                              Data="M 16,1 A 15,15 0 0 1 31,16" 
                              RenderTransformOrigin="0.5,0.5">
                            <Path.RenderTransform>
                                <RotateTransform Angle="0"/>
                            </Path.RenderTransform>
                        </Path>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever">
                                    <DoubleAnimation Storyboard.TargetName="path" 
                                                     Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" 
                                                     From="0" To="360" Duration="0:0:1"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- EmptyState Style -->
    <Style x:Key="FlatEmptyState" TargetType="HeaderedContentControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="HeaderedContentControl">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Path Data="M20 2H4c-1.1 0-1.99.9-1.99 2L2 22l4-4h14c1.1 0 2-.9 2-2V4c0-1.1-.9-2-2-2zm-7 12h-2v-2h2v2zm0-4h-2V6h2v4z"
                              Fill="{StaticResource BorderBrush}" Width="64" Height="64" Stretch="Uniform" Margin="0,0,0,10"/>
                        <ContentPresenter ContentSource="Header" TextBlock.Foreground="{StaticResource TextSecondaryBrush}" 
                                          TextBlock.FontSize="{StaticResource FontSizeMedium}" HorizontalAlignment="Center"/>
                        <ContentPresenter ContentSource="Content" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Skeleton Style -->
    <Style x:Key="FlatSkeleton" TargetType="Control">
        <Setter Property="Background" Value="{StaticResource RegionBrush}"/>
        <Setter Property="Height" Value="16"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Control">
                    <Border Background="{TemplateBinding Background}" CornerRadius="2">
                        <Border x:Name="shimmer" Background="{StaticResource BackgroundBrush}" Opacity="0.5" Width="100" HorizontalAlignment="Left">
                            <Border.RenderTransform>
                                <TranslateTransform X="-100"/>
                            </Border.RenderTransform>
                        </Border>
                    </Border>
                    <ControlTemplate.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever">
                                    <DoubleAnimation Storyboard.TargetName="shimmer" 
                                                     Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" 
                                                     From="-100" To="300" Duration="0:0:1.5"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>